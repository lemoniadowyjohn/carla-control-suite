# PROMPT P2 — FRESH CLAUDE CODE PROCESS — Hook Verification

> Generated by Claude Opus 4.8 (the P0 session) on 2026-07-30, grounded in the **verified live state** of
> worktree `carla_-main` @ `integration/governed-map-quality-20260729`.
> This concretizes P2 from `osm_carla_bundle_current_state_adjustments_v4.md`.
> **Run this in a NEW Claude Code process — NOT the session that performed P0.** A fresh process is the whole point:
> the P0 session cached the pre-`ee8871c8` hook config at startup and still emits `python3: command not found`;
> only a new process loads the patched manifest.

## Model / difficulty
- Model: **cheapest Claude Code model available** — order: **Claude Haiku 4.5 → Sonnet 5 → Opus 4.8** (only if cheaper are unavailable).
- Reasoning strength is irrelevant. A **fresh process** is mandatory.
- Difficulty: **1/10**.

## Where to run
```
WORKTREE : C:/Users/admin/PycharmProjects/gpt4/pythonProject3/carla_-main
BRANCH   : integration/governed-map-quality-20260729   (its current pushed tip; >= 42f7b77c)
```

## Objective (only this)
Confirm the **active** Claude hook configuration no longer invokes an unavailable `python3`.
Do NOT inspect architecture. Do NOT change source. Do NOT change Git state. Do NOT repair the hook in this session.

## What the fix looks like (so you know what "healthy" is)
- `.claude/run_python_hook.sh` is the portable launcher: resolves `CLAUDE_HOOK_PYTHON → $CLAUDE_PROJECT_ROOT/.venv/Scripts/python.exe → py -3 → python → python3`, silences interpreter-resolution stderr, and `exit 0` if none found (fails open, never crashes with `python3: command not found`).
- Landed in commit `ee8871c8 fix(governance): make active Claude hooks interpreter-portable` (ancestor of HEAD).
- `.claude/settings.local.json` defines **no hooks** (its `Bash(python3:*)` is only a permission-allow entry, not a hook). Any real `python3` crash therefore originates from a **user-level or plugin-level** hook manifest, or from a stale cached process.

## Procedure
1. Close all old Claude Code processes (especially the P0 session).
2. Start a **new** Claude Code process in the worktree above.
3. Record: process/session start time; worktree; branch; `HEAD` SHA; and hashes of the active hook manifests:
   ```
   .claude/settings.local.json
   .claude/run_python_hook.sh
   ~/.claude/settings.json                (user-level hooks, if present)
   ~/.claude/plugins/**/hooks/hooks.json  (plugin hook manifests, if present)
   ```
4. Trigger harmless operations that fire each hook event, and capture ALL hook stdout+stderr:
   - `PreToolUse:Read` + `PostToolUse:Read`  (read any small file)
   - `PreToolUse:Grep` + `PostToolUse:Grep`  (grep a trivial pattern)
   - `PreToolUse:Bash`/`PowerShell` + `PostToolUse` (run `echo ok`)
   - `Stop`  (end a turn)
5. Additionally, statically confirm no active hook command calls a bare `python3` instead of routing through
   `run_python_hook.sh`:
   ```
   grep -R "python3" ~/.claude/settings.json ~/.claude/plugins 2>/dev/null | grep -v run_python_hook
   ```

## Acceptance
- **PASS** only when **no** event emits `python3: command not found` (or any interpreter-not-found error), across all events in step 4.
- **FAIL** if hooks were **disabled** or replaced by unconditional success (a silenced/no-op hook is not a pass — the hook must still actually fire and produce its normal output/exit).

## Required report
```
reports/fresh_claude_hook_test/FCH01_hook_events.md
reports/fresh_claude_hook_test/FCH01_hook_events.json
```
Include: session start, worktree, branch, HEAD SHA, manifest hashes, per-event captured output, and the static-grep result.

## Verdicts (choose one)
```
FRESH_SESSION_HOOKS_PASS
FAIL_PYTHON3_STILL_ACTIVE
FAIL_HOOK_DISABLED
BLOCKED_EXTERNAL_CONFIGURATION
```

## Final block
```
FRESH CLAUDE HOOK VERDICT:

SESSION START:
WORKTREE:
BRANCH:
SHA:
HOOK EVENTS:              (which of Read/Grep/Bash/Stop fired)
HOOK ERRORS:             (verbatim, or NONE)
ACTIVE PYTHON COMMAND:   (what the launcher resolved to — venv python.exe expected)
HOOKS DISABLED:          (YES/NO — must be NO)
GOV-HOOK-001 STATUS:     (RESOLVED only if FRESH_SESSION_HOOKS_PASS; else REGRESSED / FIXED_PENDING_FRESH_SESSION)
NEXT SAFE MODEL:         (on PASS -> Codex 4.4 P3 base closure may set GOV-HOOK-001=RESOLVED)
```

## Downstream effect
- On `FRESH_SESSION_HOOKS_PASS` → this is the evidence that lets **P3 (Codex 4.4 base closure)** set
  `GOV-HOOK-001 = RESOLVED` in the ledger. Until then, P3 must keep it `FIXED_PENDING_FRESH_SESSION`.
- On `FAIL_PYTHON3_STILL_ACTIVE` → identify the exact offending hook manifest (from step 5), repair ONLY that
  manifest to route through `.claude/run_python_hook.sh`, then re-run P2. Do not touch source or Git otherwise.

Stop after the hook verdict block.
