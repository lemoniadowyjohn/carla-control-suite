# PROMPT P3 — CODEX 4.4 LIGHT — Close the Published Base (Canonical Lock + agent_sync + Ledger)

> Generated by Claude Opus 4.8 (the P0 session) on 2026-07-30, grounded in the **verified live state** of
> worktree `carla_-main` @ `integration/governed-map-quality-20260729` `42f7b77c`.
> This concretizes P3 from `osm_carla_bundle_current_state_adjustments_v4.md`. It is bounded governance/base
> work only — **no map mutation, no artifact-transaction implementation, no Unreal/CARLA.**

## Model / difficulty
- Model: **Codex 4.4 Light**
- Difficulty: **4/10**

## Pinned base (this is what unblocks the earlier `BLOCKED_INVALID_BASE`)
```
REPOSITORY : C:/Users/admin/PycharmProjects/gpt4/pythonProject3/carla_-main
WORKTREE   : (same)
BRANCH     : integration/governed-map-quality-20260729
BASE SHA   : 42f7b77cc5e78f58fb7b7cd8f7997c94101f1580   (local == origin, upstream set, 0/0)
```
Do **not** create a new parallel integration branch. Work on this branch (or an isolated worktree of it
only if this branch is already assigned to another live writer — it is not).

## What already exists — WIRE TO IT, DO NOT REINVENT
Verified tracked at `42f7b77c`:
- `ultimate_pipeline/contracts/writer_lock.py` — canonical lock impl. Constants: `LOCK_DIR=".agent_locks"`,
  `LOCK_FILE="writer.lock"`, `CANONICAL_LOCK_PATH=.agent_locks/writer.lock`, `SCHEMA_VERSION="agent-writer-lock/v1"`,
  `DEFAULT_LEASE_MINUTES=240`. API: `WriterLock.acquire(root,branch,head_sha,owner,purpose,model,task_id,
  lease_minutes,allowed_paths,forbidden_paths)`, `.save()/.load()/.release()/.heartbeat()/.is_live()/
  .is_malformed()/.owned_by()/.overlaps_path()`.
- `ultimate_pipeline/contracts/agent_sync.py` — Pydantic schema. `LockPolicyContract.lock_file`
  **already defaults to `.agent_locks/writer.lock`** (canonical). Helpers: `AgentSyncContract().to_yaml()`,
  `.save(path)`, `load_agent_sync(path=None)`, and CLI `python -m ultimate_pipeline.cli doctor --init-agent-sync`.
- `tests/unit/test_writer_lock.py` — existing lock tests (keep green).

Missing (these are your deliverables): `agent_sync.yaml`, `.agent_locks/writer.lock` (see decision below),
`AGENT_TASK_LEDGER.md`, `reports/CODEX_FIX_PROMPTS.md`, and an `agent_sync` contract test.

## Hard constraints
- **Single writer.** Acquire the canonical lock FIRST (see step 1). Fail closed on any live conflicting lock.
- **Stage only required files.** NEVER stage: `nul`, `vehicle.`, `.idea/`, `__pycache__/`, `.pytest_cache/`,
  `external/`, `carla_governed/`, generated runtime dirs, large XODR/datasets, secrets/tokens, editor files.
- **No map mutation, no artifact-transaction code, no Unreal/CARLA, no geometry edits.**
- **Portability:** all commands must work under Windows PowerShell + the repo `.venv`
  (`.\.venv\Scripts\python.exe`). Do not assume `python3`.
- **Prohibited git ops:** `reset --hard`, `clean -fdx`, forced checkout/branch-delete, destructive rebase,
  force push, blanket untracked deletion, overwriting another worktree.

## Step 1 — Acquire the canonical writer lock
Use the existing impl (do not invent a format). Recommended one-shot:
```
.\.venv\Scripts\python.exe -c "from pathlib import Path; from ultimate_pipeline.contracts.writer_lock import WriterLock; WriterLock.acquire(Path('.'), 'integration/governed-map-quality-20260729', '42f7b77c', owner='Codex 4.4 Light', purpose='P3 base closure: agent_sync.yaml + lock policy + ledger + CODEX_FIX_PROMPTS', model='codex-4.4-light', task_id='P3-BASE-CLOSURE', allowed_paths=['agent_sync.yaml','.agent_locks/*','AGENT_TASK_LEDGER.md','reports/**','tests/unit/test_agent_sync_contract.py'], forbidden_paths=['ultimate_pipeline/**/*.py','cities/**','**/*.xodr'])"
```
If a **live** lock already exists → STOP, verdict `BLOCKED_CONCURRENT_WRITER`. If a malformed/expired lock
exists, `acquire()` handles it. Record the lock JSON in BC02.

## Step 2 — DECISION: is the live `writer.lock` tracked or ignored?
A live lock holds `pid` / `expires_at` / `status: active` — ephemeral. Committing an *active* lock encodes a
perpetually-held lock and breeds merge conflicts.
- **RECOMMENDED (A):** gitignore live locks. Commit `.agent_locks/README.md` (policy + schema pointer to
  `writer_lock.py`) and add `.gitignore` rule `.agent_locks/*.lock`. Optionally commit
  `.agent_locks/writer.lock.example` with `status: released`. The live `writer.lock` stays untracked/runtime.
- **Literal alternative (B):** commit `.agent_locks/writer.lock` with `status: "released"` as a canonical
  baseline that agents overwrite at runtime. Simpler, but risks committing stale locks.
Default to **(A)** unless the requester says otherwise; state which you used in BC02.

## Step 3 — Create the canonical `agent_sync.yaml` from the schema (guaranteed valid)
Do not hand-author. Generate from the contract so `lock_file` is the canonical `.agent_locks/writer.lock`:
```
.\.venv\Scripts\python.exe -c "from ultimate_pipeline.contracts.agent_sync import AgentSyncContract; AgentSyncContract().save('agent_sync.yaml')"
```
(or `python -m ultimate_pipeline.cli doctor --init-agent-sync` if that path is wired). Then fill the domain
values that the default contract leaves blank, using the **verified reference**
`work/codex-full-pipeline-rerun-20260427/agent_sync.yaml` (fixed bbox; sensor_rig
`use_K_undistortion=true / ignore_K=true / ignore_D=true / ctv_inverted=false / vtl_inverted=true`;
determinism min 5/pref 10; `entrypoint_policy.canonical_entrypoint="python -m ultimate_pipeline.cli"`;
`forbidden_imports:[config.settings]`). **Ensure `lock_policy.lock_file: .agent_locks/writer.lock`** (canonical)
and record `.agent_lock.json` only as a legacy alias in a comment.
Verify it loads: `load_agent_sync('agent_sync.yaml')` must return a valid `AgentSyncContract` with
`lock_policy.lock_file == ".agent_locks/writer.lock"`.

## Step 4 — Governance ledger `AGENT_TASK_LEDGER.md`
Create at repo root (do not delete history if importing from a sibling copy such as
`carla_main_governed/AGENT_TASK_LEDGER.md`). Must contain: operating rules (acquire canonical lock; one writer;
immutable-parent→candidate→validation→promotion), status vocabulary, per-task **model-routing table**
(difficulty→model tier), and current rows:
```
GOV-HOOK-001 = FIXED_PENDING_FRESH_SESSION   # NOT resolved — P2 fresh-hook test has not passed yet
GOV-AUTH-001 = RESOLVED                       # base+upstream+clean proven (SHA 42f7b77c local==origin)
S01 artifact transactions = READY            # not IN_PROGRESS; implementation deferred to P5 Codex 5.5
real map mutation = BLOCKED
```
**Do NOT set `GOV-HOOK-001 = RESOLVED`** — that requires a passing P2 fresh-session hook test, which has not run.

## Step 5 — `reports/CODEX_FIX_PROMPTS.md`
Add the master delegation index (P0..P5 status + pointers). Mark P0 = INTEGRATION_BRANCH_PUSHED_GREEN
(`42f7b77c`), P1 = present (`reports/delta_base_verification/DV01–DV04`), P2 = PENDING (fresh session),
P3 = this task, P4/P5 = BLOCKED on their prerequisites.

## Step 6 — Tests
Add `tests/unit/test_agent_sync_contract.py` proving:
- `load_agent_sync('agent_sync.yaml')` succeeds and round-trips;
- `lock_policy.lock_file == ".agent_locks/writer.lock"` (canonical);
- schema rejects a malformed contract (missing/invalid field);
- (lock) `WriterLock.acquire` then a second `acquire` while live → raises; wrong-owner `release` is rejected;
  malformed lock is replaced. (Extend `test_writer_lock.py` only if a case is missing.)
Run, in order, with `UP_DISABLE_CARLA=1` and the repo venv:
```
.\.venv\Scripts\python.exe -m pytest tests/unit/test_writer_lock.py tests/unit/test_agent_sync_contract.py -v
.\.venv\Scripts\python.exe -m pytest -q          # full configured suite (pytest.ini testpaths); must be >= 323 passed, 0 failed
```

## Step 7 — Inspect, commit, push, verify
- `git status --porcelain=v2` then `git add` ONLY the required files; `git diff --cached --name-only` must show
  exactly: `agent_sync.yaml`, `.agent_locks/*` (per decision), `.gitignore` (if edited), `AGENT_TASK_LEDGER.md`,
  `reports/CODEX_FIX_PROMPTS.md`, `reports/base_closure/*`, `tests/unit/test_agent_sync_contract.py`.
- Commit structure (atomic):
  ```
  feat(governance): finalize canonical writer-lock contract + agent_sync.yaml
  docs(governance): bind ledger, CODEX_FIX_PROMPTS, and base-closure evidence to 42f7b77c
  ```
- `git push origin integration/governed-map-quality-20260729`
- Verify `git rev-parse HEAD` == `git rev-parse origin/integration/governed-map-quality-20260729`.
- Release the lock (`WriterLock.load(...).release()`), or let the lease expire; note in BC06.

## Required reports (write these)
```
reports/base_closure/BC01_branch_and_upstream.md/.json    # base SHA, upstream, local==remote, clean
reports/base_closure/BC02_lock_system.md/.json            # canonical lock, tracked-vs-ignored decision, lock JSON
reports/base_closure/BC03_agent_sync.md/.json             # YAML generated from schema; lock_file canonical; load OK
reports/base_closure/BC04_ledger.md/.json                 # ledger rows; GOV-HOOK-001=FIXED_PENDING_FRESH_SESSION
reports/base_closure/BC05_tests.md/.json                  # focused + full suite counts (fresh output)
reports/base_closure/BC06_final_status.md/.json           # final SHAs, push, lock release
```

## Verdicts (choose one)
```
BASE_READY_FOR_ARCHITECTURE_GATE
FAIL_UPSTREAM
FAIL_DIRTY_BASE
FAIL_LOCK_GOVERNANCE
FAIL_AGENT_SYNC
FAIL_TESTS
BLOCKED_CONCURRENT_WRITER
```

## Final status block
```
CODEX 4.4 BASE-CLOSURE VERDICT:

WORKTREE:
BRANCH:
BASE SHA:
FINAL LOCAL SHA:
FINAL REMOTE SHA:
LOCAL/REMOTE MATCH:
DIRTY STATE:

CANONICAL LOCK:            (.agent_locks/writer.lock — tracked? A or B)
LEGACY LOCK:               (.agent_lock.json = alias only)
LOCK TESTS:
AGENT_SYNC:                (lock_file == .agent_locks/writer.lock ? load OK ?)
GOVERNANCE LEDGER:
HOOK STATUS:               (must be FIXED_PENDING_FRESH_SESSION, not RESOLVED)
FULL OFFLINE TESTS:        (>= 323 passed, 0 failed)
COMMITS:
NEXT SAFE MODEL:           (P2 fresh Claude hook test, then P4 architecture gate in a FRESH Claude session)
```
Stop after the base-closure status block. Do not start P4/P5, Unreal, or CARLA.
