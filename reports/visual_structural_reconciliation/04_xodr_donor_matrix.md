# 04 — XODR Donor Matrix (coordinator view)

**STATUS: PENDING** — coordinator synthesis of **DSV02_xodr_donor_matrix.json**.
Best-donor-per-subsystem (OSM validation · OSM→XODR · canonical geometry · topology · lanes · elevation ·
artifact transactions · CARLA standalone load) decided ONLY after DSV02. `run_11` XODR is historical-only and is
NOT eligible as the new structural base. Do not pre-select.
