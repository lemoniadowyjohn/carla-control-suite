# 01 — Worktree × Subsystem Matrix

**STATUS: PENDING** — filled by Claude synthesis after **DSV01** + **DSV02** complete.

This matrix (worktree × subsystem → evidence class + best-donor flag) must NOT be fabricated pre-discovery.
Inputs: `DSV01_visual_donor_matrix.json`, `DSV02_xodr_donor_matrix.json`.
Candidate donor worktrees identified in R00 (to be evaluated, not pre-selected): `carla_rr_recovery`,
`carla_main_governed/work/codex-full-pipeline-rerun-20260427`, `carla_main_governed_worktrees/codex-jsnap-20260428`,
`carla_main_governed` (dirty; read-only). See `R00_worktrees.md`.
