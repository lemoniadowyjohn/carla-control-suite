# 02 — Artifact Lineage

**STATUS: PENDING** — filled after DSV01/DSV02 (hash-based lineage).

Must trace, by **sha256** (not filename), the lineage: OSM → XODR (+ manifests) and OSM → OSM2World (OBJ/GLB) → Blender → FBX.
Preserve and record (do not modify): `run_11`, `08_final_structural_gap.xodr`, `scenario_b_audit/contract_run/`,
`submission/results/structural_gap_run11/`. Inputs: `DSV01_*`, `DSV02_*`.
