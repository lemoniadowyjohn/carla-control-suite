# 03 — Visual Donor Matrix (coordinator view)

**STATUS: PENDING** — this is the coordinator's synthesis of **DSV01_visual_donor_matrix.json**.
Best-donor-per-subsystem (OSM2World exec · Blender exec · semantic splitting · FBX export · FBX validation · Unreal import)
is decided ONLY after DSV01 returns with hashes + evidence classes. Do not pre-select. See `08_delegation_plan.md`.
