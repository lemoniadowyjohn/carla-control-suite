# Codex — 2 more findings, plus 1 already-drafted prompt now unblocked

## PROMPT F — GAP-018: root-cause the 180-degree tangent reversal on road 45622

```text
Repository: lemoniadowyjohn/carla-control-suite. Prerequisite: Wave 1's geometry kernel
(feature/geometry-kernel-v1-20260907, verified PASS) -- use it for any pose/curvature evaluation
in this investigation, do not hand-roll a new one. Isolated branch:
feature/gap-018-tangent-reversal-<date>.

CONFIRMED (this session's audit): road 45622, geometry index 60->61, on the current pinned
map-of-record has a genuine 179.9999-degree tangent reversal -- verified by direct polynomial
evaluation (not just a checker artifact): the paramPoly3's own du/dp goes negative within its
[0,1] domain, meaning the curve's own u-parametrization folds back on itself. This is flagged by
check_planview_internal_seams' heading-only bucket (diagnostic/non-gating by design -- 82 total
heading-snap discontinuities, this is the clear standout).

TASK:
1. Extract road 45622's exact paramPoly3 coefficients from the pinned map-of-record
   (campaigns/ingolstadt_cooked_perception_v1/candidate/ingolstadt_perception_map_of_record_20260905_202847.xodr)
   and confirm the du/dp sign-reversal directly, using the geometry kernel.
2. Trace which upstream stage produced this specific curve fit (osm2odr conversion? a
   post-processing smoothing/repair stage? one of the ENABLE_UNSAFE_* experimental flags, though
   those should be off by default on this map -- confirm they were off for this regen). The gap
   register notes this wasn't identified in the prior pass -- you need to actually find it.
3. If the root cause is in this codebase's own post-processing (not the upstream OSM->OpenDRIVE
   converter itself), fix it and add a regression test using road 45622's actual coefficients as
   the fixture. If the root cause is upstream (in the converter, outside this codebase's control),
   do not attempt to patch around it blindly -- instead, add a targeted repair specifically for
   this defect class (du/dp sign reversal within a paramPoly3's domain) to whichever geometry
   repair stage already handles similar cases, with the same regression fixture.
4. Either way: confirm road 45622 no longer exhibits the reversal after your fix, re-run
   check_planview_internal_seams against the pinned map, and report the new heading-snap-
   discontinuity count (should be 81, down from 82, with zero new discontinuities introduced
   elsewhere).

This is a single confirmed instance, not a systemic pattern search -- do not scope-creep into
auditing all 82 discontinuities in this pass. If you find evidence during root-causing that other
roads share the exact same failure mode (du/dp sign reversal, not just any heading discontinuity),
report that as a finding but do not attempt to fix them all in this task.

End with:
ROOT_CAUSE_IDENTIFIED: <upstream converter | this codebase's stage: name it>
FIXED: PASS | FAIL | UPSTREAM_NOT_FIXABLE_HERE
ROAD_45622_REVERSAL_RESOLVED: PASS | FAIL
HEADING_SNAP_DISCONTINUITY_COUNT: <before> -> <after>
OTHER_INSTANCES_FOUND: <count, or NONE -- not fixed in this pass either way>
FULL_OFFLINE_TESTS: PASS | FAIL
```

---

## PROMPT G — GAP-013: wire Phase J visual/collision QA into the live pipeline

```text
Repository: lemoniadowyjohn/carla-control-suite. Isolated branch: feature/phase-j-wiring-v1-<date>.

CONFIRMED (this session's audit): ultimate_pipeline/tools/phase_j_osm2world_blender.py (stages
J1-J8: OSM2World conversion, Blender import, FBX export, collision-mesh generation, detached-slab
/z-fighting checks) is real, substantive code with genuine execution evidence in
reports/post_audit_hardening/{20260804T125500Z,20260804T130959Z,20260804T185517Z}/PHASE_J_OSM2WORLD_BLENDER.json
(verdict J1_PASS/J4_OK/J5_ALIGNED/J6_PASS/J7_PASS/J8_PASS). But main_pipeline.py imports
OSM2WorldRunner/is_osm2world_enabled and NEVER calls them anywhere in the pipeline body (import
only, zero call sites). The validated scene in the existing evidence is smoke-test scale (19
vertices, 17 faces, 3 objects: 2 placeholder + 1 ground plane) -- this has never run at the real
map's scale (~149MB, tens of thousands of roads).

Also note: reports/post_audit_hardening/ (where the J-stage evidence lives) is otherwise
frozen/append-only per REPOSITORY_GOVERNANCE.md -- you are reading from it, not writing new
material into old timestamped directories; write any new evidence this task produces under a
fresh, clearly-dated subdirectory, following the existing naming convention.

TASK:
1. Wire OSM2WorldRunner into main_pipeline.py's actual pipeline body, gated behind
   is_osm2world_enabled() (an opt-in flag, not on by default -- this depends on an external
   OSM2World binary/toolchain being present, confirm what happens gracefully when it's absent
   before assuming it always runs).
2. This requires OSM2World + Blender to actually be installed/available in this environment to
   run at real scale -- confirm whether that's the case before attempting a full-map run. If the
   toolchain isn't available here, wire the call site correctly (so it's no longer dead code) and
   report that full-scale validation is blocked on tooling, rather than fabricating a result.
3. If the toolchain IS available: run it against the real pinned map-of-record (not the 19-vertex
   smoke fixture) and report actual scale numbers (vertex/face/object counts, wall-clock time,
   pass/fail per J-stage). This is the first real signal on whether Phase J's checks (including
   z-fighting, per the earlier feature/z-fighting-detector-20260907 branch that was built waiting
   on this) hold up at production scale.
4. Do not silently swallow a J-stage failure at scale -- if any J-stage that passed at
   smoke-test scale fails at real scale, that is itself the most important finding of this task;
   report it clearly, do not paper over it to get a green run.

End with:
WIRED: PASS | FAIL
TOOLCHAIN_AVAILABLE: YES | NO -- <what you found>
FULL_SCALE_RUN: COMPLETED | BLOCKED_ON_TOOLCHAIN | BLOCKED_ON_OTHER (name it)
FULL_SCALE_RESULTS: <per-J-stage pass/fail + scale numbers, or N/A if blocked>
FULL_OFFLINE_TESTS: PASS | FAIL
```

---

## Already-drafted, now unblocked: Wave 3 Prompt 2 (GAP-014, completeness metric)

Fix 2 (the OSM<->XODR correspondence engine prerequisite) landed and verified clean
(osm-correspondence-engine-v1-20260907 @ 6889ffdb). Wave 3 Prompt 2 (OSM-vs-generated semantic
completeness metric) was gated on that landing and can now be sent as originally written --
no changes needed, just send it. (Full text is in CODEX_EXECUTION_PROMPTS.md, Wave 3 Prompt 2, on
the architecture/production-map-quality-20260906 branch.)

Wave 3 Prompt 1 (regulatory precedence, GAP-005 extension + GAP-040) is also technically unblocked
now for the same reason, but depends on understanding the correspondence engine's ACTUAL shipped
output schema (which changed between the original Fix 2 attempt and the corrected one) -- re-read
that prompt's own instruction to "re-verify against the merged correspondence engine's actual
output schema before starting" before sending it; it may need a light edit once someone's actually
looked at 6889ffdb's final schema.
