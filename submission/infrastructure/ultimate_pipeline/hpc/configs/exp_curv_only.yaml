name: exp_curv_only
domain_gap_enable:
  geometry: false
  curvature: true
  intersection: false
  semantics: false
  road_classification: false
  perception: false

pipeline:
  # keep everything identical to geom-only
  enable_realism: false
  enable_buildings: false
  enable_traffic_lights: false
  enable_sidewalks: false

training:
  seed: 42
