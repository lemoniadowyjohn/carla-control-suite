[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    slow: Long-running integration tests
    quality: OpenDRIVE quality-gate tests (disabled by default in CI)
    carla: Tests requiring CARLA Python API + a running simulator
