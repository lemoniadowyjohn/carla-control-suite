# Architecture

RoadRunner is an optional, license-dependent component within the workflow.

## Key Principles
- RoadRunner import/export is not assumed to be lossless.
- A .rrscene file never silently replaces the governed thesis artifact.
- MathWorks' CARLA plugin target version differs from the project's target of CARLA 0.9.16.
