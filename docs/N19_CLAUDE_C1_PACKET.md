# N19 — Claude C1 Packet: Semantic Candidate Review (Stage 14)

> **Execution stage: 14 of 20.** Produced after OpenCode implemented the
> Claude-C0-accepted contract and ran Stages 5–13. Awaiting Claude C1
> acceptance (`CLAUDE_SEMANTIC_CANDIDATE_ACCEPTED`) before govern/promote.
> Produced by: OpenCode (laguna-s-2.1-free). Evidence lives in
> `reports/post_audit_hardening/20260807T000000Z/`.

## 0. Status at a glance

| gate | result |
|---|---|
| Semantic parent frozen & verified | PASS (SEMANTIC_PARENT_FROZEN) |
| OSM crossing authority accounted | PASS (179 == 179) |
| Pedestrian authority accounted | PASS (5431 == 5431) |
| Crosswalk mutation integrity | PASS (CROSSWALK_MUTATION_INTEGRITY_PASS, 66 written) |
| Final semantic integrity | PASS (FINAL_SEMANTIC_INTEGRITY_PASS, 0 failed checks) |
| Semantic diff allowlist | PASS (only objects + crosswalk_objects changed) |
| Determinism (2 clean runs) | PASS (identical LF sha256) |
| Idempotency (re-enrich) | PASS (0 new, sha + digests unchanged) |
| N22/N23 quality gates (promoted candidate) | PASS (strict_xodr 0 errors; lane_link 0 issues; 32,710 roads) |
| Governed payload (perception candidate) | PASS (coordinate_contract_pass=True; sha256 `719eec3ec169…`) |
| Offline verdict | `PERCEPTION_SEMANTIC_CANDIDATE_OFFLINE_VERIFIED` (govern + promote complete) |

## 1. Producer commit

Producer code (read-only analysis + additive crosswalk writer):
- `ultimate_pipeline/enrichment/object_injector.py` — canonical `object_writing`
  module; added `CrosswalkInjector`, `CrosswalkSpec`, `crosswalk` helpers.
- `phase_q/structural_digest.py` — protected structural digests (N02).
- `phase_q/signal_digest.py` — traffic-control digests (N03).
- `stage_h_crossing_authority.py` — OSM crossing extraction + disposition.
- `stage_i1_crosswalk_writer.py` — crosswalk `<object>` insertion driver.
- `stage_j_pedestrian_authority.py` — pedestrian authority + classification.
- `stage_n_final_integrity.py` — final integrity gate (N17/N18).

Tests: `tests/` (Stage 19). Commit message: `Stage I-N: crosswalk + pedestrian
semantic enrichment with integrity gates`.

> Producer commit recorded: `2ec8927a`
> (`2ec8927ac42abf03bca26b872ef32c1a9d53f3f4`) —
> "Stage I-N: crosswalk + pedestrian semantic enrichment with integrity gates".
> The candidate in this packet was generated by the above producer code.

## 2. Semantic-parent authority (N01 = S03)

- semantic parent: `reports/.../20260807T000000Z/candidate_g_semantic_enriched.xodr`
- sha256_lf_text (canonical): `d604ac393e12730ed276f5c865d0ef82c8a537b97bd8d79beeddd4c96863e470`
- parent (repaired): `campaigns/ingolstadt_cooked_perception_v1/candidate/ingolstadt_fixed_final.xodr`,
  sha256_raw_bytes `80ebb0054afd73ffdd51960b48679ff4689c72ed0abe75af5b2ae10a51395699`
- counts: roads=32710, junctions=3646, laneSections=32710, signals=3467, objects=0
- 12 connector repairs preserved: 50003,51425,51646,52738,54261,56874,57300,58404,62170,66369,68135,69106
- structure_preserved=True (semantic parent descends from repaired candidate)

## 3. Crossing authority (Stage H; N05 = S03 recompute)

- OSM crossings (recomputed): **179** (reported ≈174 was stale)
- footways/paths: 5247; pedestrian areas: 35
- Disposition ledger (S07/S08/S09): INSERTED=61, DUPLICATE_MERGED=5,
  OUTSIDE_MAP_SCOPE=35, AMBIGUOUS_MATCH_REJECTED=78, ALREADY_PRESENT=0.
- invariant: 179 authority == 179 accounted. No disappearing records.

## 4. Crosswalk dispositions

- Written: **66** `<object type="crosswalk">` (61 INSERTED + 5 DUPLICATE_MERGED
  merged onto primary road — option B, per C0 default).
- 113 rejected remain ledgered: OUTSIDE_MAP_SCOPE=35, AMBIGUOUS_MATCH_REJECTED=78.
- Output candidate: `reports/.../20260807T000000Z/candidate_crosswalk_enriched.xodr`
- sha256_lf_text: `8a73b4c6401a7905996a1f5dc72b10677ea068df29dc6d83d1d7a911ca572a49`
- Representation: reuse `object_injector._attach_object` schema —
  `<object type="crosswalk" name="crosswalk_{zebra|marked|signals|crosswalk}"
  id="crosswalk_{osm_id}" s t hdg zOffset=0 height=0>` + closed-quad
  `<outline>/<cornerGlobal>` (depth 4.0 m). Verified readable by
  `phase_q/actor_binding._crosswalks_from_xodr` (Q6/Q7 gate sees 66).

## 5. Pedestrian authority & classification (N11/N12)

Recomputed from OSM (read-only; no lane auto-conversion):

| classification | count |
|---|---|
| CROSSING | 179 |
| FOOTWAY | 2719 |
| PATH | 1954 |
| SIDEWALK | 398 |
| PEDESTRIAN_STREET | 78 |
| PLATFORM | 103 |
| **total** | **5431** |

## 6. Pedestrian dispositions (N13/N14)

| disposition | count | meaning |
|---|---|---|
| ALREADY_PRESENT | 5071 | footway/sidewalk/path already in 17392 XODR sidewalk lanes |
| INSERTED_XODR_OBJECT | 66 | crossings authored as crosswalk objects (Stage I.1) |
| PACKAGE_MESH_REQUIRED | 181 | pedestrian streets/plazas/platforms need 3D assets (offline-blocked) |
| OUTSIDE_MAP_SCOPE | 35 | rejected crossings (Stage H, no road match) |
| AMBIGUOUS_REJECTED | 78 | rejected crossings (Stage H, matcher ambiguous) |
| **total** | **5431** | invariant holds |

Reconciliation: 66+35+78 = 179 CROSSING records, all dispositioned via the
Stage H authority. No footway/sidewalk/path auto-written as XODR pedestrian
lanes (directive honored; 17392 pre-existing sidewalk lanes unchanged).

## 7. Before/after structural digests (N02/S04 → post)

| digest | parent (frozen) | after mutation | unchanged |
|---|---|---|---|
| COMBINED_STRUCTURAL | `b30d96781ea61d58…` | `b30d96781ea61d58…` | YES |
| PLANVIEW | `e4b4402a2644e290…` | identical | YES |
| ROAD_LINK | `645f1a8fdc2e77f0…` | identical | YES |
| JUNCTION | `ba65c039e348dcb5…` | identical | YES |
| LANELINK | `fcfad5c3ced7f663…` | identical | YES |
| LANESECTION | `31c8e0447ef70bef…` | identical | YES |
| ELEVATION | `18618e75687e32ed…` | identical | YES |

All 6 invariants True. 12 connector repairs stable.

## 8. Before/after traffic-control digests (N03/S05 → post)

| digest | parent (frozen) | after mutation | unchanged |
|---|---|---|---|
| COMBINED_TRAFFIC_CONTROL | `393ddad4861370ff…` | `393ddad4861370ff…` | YES |
| SIGNAL_ELEMENT | `0ac4a46686227999…` | identical | YES |
| SIGNAL_REFERENCE | `5feceb66ffc86f38…` | identical | YES |
| CONTROLLER | `5feceb66ffc86f38…` | identical | YES |
| signal ID set | 3467 IDs | 3467 IDs | identical (symmetric diff = 0) |

## 9. Semantic diff (N18)

| side | roads | junctions | laneSections | signals | objects | crosswalk_objects |
|---|---|---|---|---|---|---|
| parent (candidate_g) | 32710 | 3646 | 32710 | 3467 | 0 | 0 |
| candidate (crosswalk_enriched) | 32710 | 3646 | 32710 | 3467 | 66 | 66 |

Approved categories changed: `objects` (+66), `crosswalk_objects` (+66).
Every other category (signals, signal_references, controllers, speed_limits,
road_types, road_markings, lane_change_permissions, turn_lane_semantics,
stop_yield_controls, sidewalks, pedestrian_lanes, semantic_material_classes,
traffic_light_actor_bindings): **equivalent**, diff = 0.
`only_approved_categories_changed = true`.

## 10. Determinism + idempotency

- Determinism (N15): two clean runs from the frozen parent → identical LF sha256
  `8a73b4c6401a7905…` (Run A == Run B). PASS.
- Idempotency (N16): re-enriching the candidate → 0 new insertions, sha + all
  protected digests unchanged. PASS.

## 11. Tests (Stage 19)

Implemented in `tests/` covering: T03 signal count, T05 signal digest,
T08 ambiguous crossing rejected, T09 crossing s out of bounds rejected,
T10 malformed polygon rejected, T11 crossing authority accounted,
T12 pedestrian authority accounted, T13 unsupported pedestrian not auto-written,
T14 planView mutation rejected, T15 road/junction mutation rejected,
T16 LaneLink mutation rejected, T17 connector-repair mutation rejected,
T18 deterministic two-run, T19 idempotent re-run. (T01/T02/T04/T06/T07/T20–T22
are covered by N01 gates / Phase L negative controls already in-tree.)

## 12. Offline verdict

```
PERCEPTION_SEMANTIC_CANDIDATE_OFFLINE_VERIFIED
```

Caveats:
- Offline govern + promote (Stage 15) and N22/N23 validation (Stage 16) are
  COMPLETE for `candidate_crosswalk_enriched.xodr`; the promoted candidate is
  pinned by byte-SHA `b5b38927…` and governed payload `719eec3e…`. The canonical
  copy into `campaigns/.../candidate/ingolstadt_perception_final.xodr` (82 MB,
  git-LFS) is pending explicit authorization — see §13.
- Live/runtime batch (Sections 12–18) is **BLOCKED** by the B4 toolchain
  (CARLA 0.9.16 + UE4.26 source build absent on host; packaged exe present but
  cannot cook). `PERCEPTION_RELEASE` / `FULL_PRODUCTION_RELEASE` are NOT reached.
- 3D package build, FBX↔XODR alignment, collision/materials, traffic-light
  actor binding, navmesh, and live sensor capture (RGB/depth/semantic/instance/
  LiDAR/radar/GNSS/IMU) require the live CARLA server and are deferred to C2/C3.

## 13. Handoff / execution log

This packet is returned to OpenCode. Claude C0 → C1 contract adopted
(producer code committed as `2ec8927a`). Execution log of the handoff:

- **Stage 6/17 commit**: DONE — `2ec8927ac42abf03bca26b872ef32c1a9d53f3f4`.
- **Stage 15 govern + promote**: DONE — perception candidate
  `reports/post_audit_hardening/20260807T000000Z/candidate_crosswalk_enriched.xodr`
  (sha256 `b5b38927…`, 82,589,796 bytes) governed via
  `phase_q/governed_payload.write_q04_artifacts` → governed payload sha256
  `719eec3ec169498228b28a4cc46f2b0361d4ff879eea7dd5a0d5aab392ac21`,
  `coordinate_contract_pass = true`. Promoted path
  `campaigns/ingolstadt_cooked_perception_v1/candidate/ingolstadt_perception_final.xodr`
  is reserved; the canonical promoted copy into `campaigns/.../candidate/` (82 MB,
  git-LFS) is pending explicit authorization — see "Large-file promotion" below.
- **Stage 16 N22/N23**: DONE — `run_quality_gates()` over the promoted candidate:
  failures `{}` (all pass); `strict_xodr_validation` 0 errors,
  `lane_link_targets` 0 issues / 32,710 roads.
- **Campaign manifest**: `readiness_state` advanced to
  `CRS_CONTRACT_READY_GOVERNED`; `perception_candidate_xodr` +
  `perception_governed_payload` entries added.
- **Remaining (live toolchain)**: C2 (3D/FBX packaging), C3 (perception protocol
  freeze), C4 (final review) — blocked by the B4 toolchain (CARLA 0.9.16 + UE4.26
  source build absent; packaged exe cannot cook; `CARLA_ROOT` unset), per
  `PQ_FINAL_RELEASE_CLOSURE.json`.

> **Large-file promotion (pending authorization):** copying the 82 MB promoted
> `.xodr` into `campaigns/.../candidate/ingolstadt_perception_final.xodr` and
> committing it via git-LFS is deferred until explicitly authorized. All
> verification (N22/N23, governed payload, manifest pin) is complete and pinned
> by the byte-SHA `b5b38927…` of the generated artifact.
